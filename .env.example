# Copy this file to `.env` and fill in your own token.
# Never commit `.env`.

HF_TOKEN=hf_xxx_replace_with_your_read_token
# Optional mirror variable; train_sft.py reads either one.
# HUGGINGFACE_HUB_TOKEN=hf_xxx_replace_with_your_read_token
